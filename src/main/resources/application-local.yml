# application-local.yml dziedziczy z application.properties więc wpisujemy do niego tylko unikalne propertisy
server:
  port: 8090

spring:
  h2:
    console:
      enabled: true
      path: '/console'
  datasource:
    url: 'jdbc:h2:file:./todo-db'
  jpa:
    hibernate:
      ddl-auto: validate

